# PDF Split Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a browser-based PDF splitting tool with three modes (extract every page, split by range, select pages), thumbnail preview, and ZIP download — all client-side with zero new dependencies.

**Architecture:** Single-tool page following the pdf-merge pattern exactly. Core split logic in `libs/pdf-split/split.ts` using pdf-lib's `copyPages` API. Thumbnail rendering reuses the same pdfjs-dist canvas pattern from pdf-merge but accepts a `pageIndex` parameter. UI is a three-component client page (`Conversion`, `Description`, `PdfSplitPage`) with three visual states (empty → loaded → results).

**Tech Stack:** Next.js 16 App Router, TypeScript, Tailwind CSS 4, pdf-lib 1.17.1, pdfjs-dist 4.10.38, fflate ^0.8.2, file-selector, vitest

---

## File Structure

```
app/[locale]/pdf-split/
├── page.tsx                          # Route entry (Task 1)
└── pdf-split-page.tsx                # Client page component (Task 4)

libs/pdf-split/
├── split.ts                          # Core split logic + getPdfPageCount (Task 2)
├── thumbnail.ts                      # Page thumbnail rendering (Task 2)
└── __tests__/
    └── split.test.ts                 # Unit tests for split logic (Task 3)
```

**Modified files:**

- `libs/tools.ts` — Add Scissors import, TOOLS entry, TOOL_CATEGORIES, TOOL_RELATIONS (Task 5)
- `vitest.config.ts` — Add `libs/pdf-split/**/*.test.ts` to include (Task 3)
- `public/locales/en/tools.json` — Add pdf-split entry (Task 6)
- `public/locales/en/pdf-split.json` — New file (Task 6)
- `public/locales/{zh-CN,zh-TW,ja,ko,es,pt-BR,fr,de,ru}/tools.json` — Add pdf-split entry (Task 7)
- `public/locales/{zh-CN,zh-TW,ja,ko,es,pt-BR,fr,de,ru}/pdf-split.json` — New files (Task 7)

---

### Task 1: Route Entry Page

**Files:**

- Create: `app/[locale]/pdf-split/page.tsx`

- [ ] **Step 1: Create the route entry file**

This file follows the exact same pattern as `app/[locale]/pdf-merge/page.tsx`. It provides `generateMetadata` for SEO and renders `<PdfSplitPage />` with JSON-LD schemas.

```typescript
import { getTranslations } from "next-intl/server";
import { generatePageMeta } from "../../../libs/seo";
import { buildToolSchemas } from "../../../components/json-ld";
import { TOOLS, TOOL_CATEGORIES, CATEGORY_SLUGS } from "../../../libs/tools";
import PdfSplitPage from "./pdf-split-page";

const PATH = "/pdf-split";
const TOOL_KEY = "pdf-split";
const tool = TOOLS.find((t) => t.key === TOOL_KEY)!;

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  return generatePageMeta({
    locale,
    path: PATH,
    title: t("pdf-split.title"),
    description: t("pdf-split.description"),
    ogImage: { type: "tool", key: TOOL_KEY },
  });
}

export default async function PdfSplitRoute({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  const tx = await getTranslations({ locale, namespace: "pdf-split" });
  const tc = await getTranslations({ locale, namespace: "categories" });
  const category = TOOL_CATEGORIES.find((c) => c.tools.includes(TOOL_KEY))!;
  const categorySlug = CATEGORY_SLUGS[category.key];

  const howToSteps = Array.from({ length: 3 }, (_, i) => ({
    name: tx(`descriptions.step${i + 1}Title`),
    text: tx(`descriptions.step${i + 1}Text`),
  })).filter((step) => step.name);

  const schemas = buildToolSchemas({
    name: t("pdf-split.title"),
    description: tx.has("descriptions.aeoDefinition")
      ? tx("descriptions.aeoDefinition")
      : t("pdf-split.description"),
    path: PATH,
    categoryName: tc(`${category.key}.shortTitle`),
    categoryPath: `/${categorySlug}`,
    faqItems: [1, 2, 3].map((i) => ({
      q: tx(`descriptions.faq${i}Q`),
      a: tx(`descriptions.faq${i}A`),
    })),
    howToSteps,
    sameAs: tool.sameAs,
  });

  return (
    <>
      {schemas.map((s, i) => (
        <script
          key={i}
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(s) }}
        />
      ))}
      <PdfSplitPage />
    </>
  );
}
```

- [ ] **Step 2: Verify the file compiles**

Run: `npx tsc --noEmit app/\\[locale\\]/pdf-split/page.tsx 2>&1 | head -20`

Note: This will fail until Task 5 (tools.ts registration) and Task 6 (en i18n) are done. The file itself is correct — defer type checking to Task 8.

- [ ] **Step 3: Commit**

```bash
git add app/\[locale\]/pdf-split/page.tsx
git commit -m "feat(pdf-split): add route entry page"
```

---

### Task 2: Core Split Logic + Thumbnail Rendering

**Files:**

- Create: `libs/pdf-split/split.ts`
- Create: `libs/pdf-split/thumbnail.ts`

- [ ] **Step 1: Create `libs/pdf-split/split.ts`**

This file contains `getPdfPageCount` and `splitPdf` — the core business logic for splitting a PDF into multiple files. It uses pdf-lib's `copyPages` API.

```typescript
import { PDFDocument } from "pdf-lib";

export type SplitMode = "extract-all" | "by-range" | "select-pages";

export interface ExtractAllOptions {
  mode: "extract-all";
}

export interface SplitByRangeOptions {
  mode: "by-range";
  ranges: { from: number; to: number }[]; // 1-indexed, inclusive
}

export interface SelectPagesOptions {
  mode: "select-pages";
  groups: number[][]; // Each group is 0-indexed page indices
}

export type SplitOptions = ExtractAllOptions | SplitByRangeOptions | SelectPagesOptions;

export interface SplitResult {
  name: string; // e.g., "pages_1-3.pdf"
  bytes: Uint8Array;
}

export interface SplitProgress {
  current: number;
  total: number;
}

export async function getPdfPageCount(data: ArrayBuffer): Promise<number> {
  const doc = await PDFDocument.load(new Uint8Array(data.slice(0)), {
    ignoreEncryption: true,
  });
  return doc.getPageCount();
}

export async function splitPdf(
  sourceData: ArrayBuffer,
  options: SplitOptions,
  onProgress?: (progress: SplitProgress) => void
): Promise<SplitResult[]> {
  const srcDoc = await PDFDocument.load(new Uint8Array(sourceData.slice(0)), {
    ignoreEncryption: true,
  });
  const totalPages = srcDoc.getPageCount();
  const results: SplitResult[] = [];

  if (options.mode === "extract-all") {
    for (let i = 0; i < totalPages; i++) {
      onProgress?.({ current: i + 1, total: totalPages });
      const newDoc = await PDFDocument.create();
      const [page] = await newDoc.copyPages(srcDoc, [i]);
      newDoc.addPage(page);
      const bytes = await newDoc.save();
      results.push({
        name: `page_${i + 1}.pdf`,
        bytes: new Uint8Array(bytes),
      });
    }
  } else if (options.mode === "by-range") {
    for (const range of options.ranges) {
      const from = Math.max(0, range.from - 1); // Convert to 0-indexed
      const to = Math.min(totalPages - 1, range.to - 1);
      if (from > to) continue;

      onProgress?.({ current: results.length + 1, total: options.ranges.length });
      const newDoc = await PDFDocument.create();
      const indices = [];
      for (let i = from; i <= to; i++) {
        indices.push(i);
      }
      const pages = await newDoc.copyPages(srcDoc, indices);
      for (const page of pages) {
        newDoc.addPage(page);
      }
      const bytes = await newDoc.save();
      results.push({
        name: `pages_${range.from}-${range.to}.pdf`,
        bytes: new Uint8Array(bytes),
      });
    }
  } else if (options.mode === "select-pages") {
    for (const group of options.groups) {
      if (group.length === 0) continue;

      onProgress?.({ current: results.length + 1, total: options.groups.length });
      const newDoc = await PDFDocument.create();
      const sortedIndices = [...group].sort((a, b) => a - b);
      const pages = await newDoc.copyPages(srcDoc, sortedIndices);
      for (const page of pages) {
        newDoc.addPage(page);
      }
      const bytes = await newDoc.save();

      // Generate filename from sorted page numbers (1-indexed)
      const pageNumbers = sortedIndices.map((i) => i + 1);
      let name: string;
      if (pageNumbers.length === 1) {
        name = `page_${pageNumbers[0]}.pdf`;
      } else {
        name = `pages_${pageNumbers[0]}-${pageNumbers[pageNumbers.length - 1]}.pdf`;
      }
      results.push({ name, bytes: new Uint8Array(bytes) });
    }
  }

  return results;
}
```

- [ ] **Step 2: Create `libs/pdf-split/thumbnail.ts`**

This file renders a single page thumbnail. It follows the same pattern as `libs/pdf-merge/thumbnail.ts` but accepts a `pageIndex` parameter (0-indexed) instead of always rendering page 1.

```typescript
export async function renderPageThumbnail(
  data: ArrayBuffer,
  pageIndex: number,
  maxWidth = 120,
  maxHeight = 160
): Promise<string> {
  const pdfjs = await import("pdfjs-dist");
  pdfjs.GlobalWorkerOptions.workerSrc = "/pdf.worker.min.mjs";

  // Copy the buffer — pdfjs-dist may transfer (detach) the underlying ArrayBuffer
  const pdf = await pdfjs.getDocument({ data: new Uint8Array(data.slice(0)) }).promise;
  const page = await pdf.getPage(pageIndex + 1); // pdfjs uses 1-indexed pages

  const viewport = page.getViewport({ scale: 1 });
  const scale = Math.min(maxWidth / viewport.width, maxHeight / viewport.height);
  const scaledViewport = page.getViewport({ scale });

  const canvas = document.createElement("canvas");
  canvas.width = scaledViewport.width;
  canvas.height = scaledViewport.height;

  const ctx = canvas.getContext("2d")!;
  await page.render({ canvasContext: ctx, viewport: scaledViewport }).promise;

  const dataUrl = canvas.toDataURL("image/png");

  pdf.destroy();
  canvas.width = 0;
  canvas.height = 0;

  return dataUrl;
}
```

- [ ] **Step 3: Commit**

```bash
git add libs/pdf-split/split.ts libs/pdf-split/thumbnail.ts
git commit -m "feat(pdf-split): add core split logic and thumbnail rendering"
```

---

### Task 3: Unit Tests

**Files:**

- Create: `libs/pdf-split/__tests__/split.test.ts`
- Modify: `vitest.config.ts` (add include path)

- [ ] **Step 1: Write the failing tests**

Follows the same pattern as `libs/pdf-merge/__tests__/merge.test.ts` — uses pdf-lib to create test PDFs in-memory, then asserts split results.

```typescript
import { describe, it, expect, vi } from "vitest";
import { PDFDocument } from "pdf-lib";
import { splitPdf, getPdfPageCount } from "../split";

async function makePdf(pageCount: number): Promise<ArrayBuffer> {
  const doc = await PDFDocument.create();
  for (let i = 0; i < pageCount; i++) {
    doc.addPage();
  }
  const bytes = await doc.save();
  return bytes.buffer as ArrayBuffer;
}

describe("getPdfPageCount", () => {
  it("returns correct count for single-page PDF", async () => {
    const pdf = await makePdf(1);
    expect(await getPdfPageCount(pdf)).toBe(1);
  });

  it("returns correct count for multi-page PDF", async () => {
    const pdf = await makePdf(42);
    expect(await getPdfPageCount(pdf)).toBe(42);
  });

  it("returns 1 for empty PDF (pdf-lib default)", async () => {
    const pdf = await makePdf(0);
    expect(await getPdfPageCount(pdf)).toBe(1);
  });

  it("throws on corrupted data", async () => {
    const corrupted = new ArrayBuffer(10);
    await expect(getPdfPageCount(corrupted)).rejects.toThrow();
  });
});

describe("splitPdf — extract-all mode", () => {
  it("extracts every page from a multi-page PDF", async () => {
    const pdf = await makePdf(5);
    const results = await splitPdf(pdf, { mode: "extract-all" });
    expect(results).toHaveLength(5);
    for (let i = 0; i < results.length; i++) {
      expect(results[i].name).toBe(`page_${i + 1}.pdf`);
      const doc = await PDFDocument.load(results[i].bytes);
      expect(doc.getPageCount()).toBe(1);
    }
  });

  it("handles single-page PDF", async () => {
    const pdf = await makePdf(1);
    const results = await splitPdf(pdf, { mode: "extract-all" });
    expect(results).toHaveLength(1);
    expect(results[0].name).toBe("page_1.pdf");
  });

  it("fires onProgress once per output page", async () => {
    const pdf = await makePdf(3);
    const progress = vi.fn();
    await splitPdf(pdf, { mode: "extract-all" }, progress);
    expect(progress).toHaveBeenCalledTimes(3);
    expect(progress).toHaveBeenCalledWith({ current: 1, total: 3 });
    expect(progress).toHaveBeenCalledWith({ current: 2, total: 3 });
    expect(progress).toHaveBeenCalledWith({ current: 3, total: 3 });
  });
});

describe("splitPdf — by-range mode", () => {
  it("splits by a single range", async () => {
    const pdf = await makePdf(10);
    const results = await splitPdf(pdf, {
      mode: "by-range",
      ranges: [{ from: 1, to: 5 }],
    });
    expect(results).toHaveLength(1);
    expect(results[0].name).toBe("pages_1-5.pdf");
    const doc = await PDFDocument.load(results[0].bytes);
    expect(doc.getPageCount()).toBe(5);
  });

  it("splits by multiple non-overlapping ranges", async () => {
    const pdf = await makePdf(10);
    const results = await splitPdf(pdf, {
      mode: "by-range",
      ranges: [
        { from: 1, to: 3 },
        { from: 4, to: 6 },
        { from: 7, to: 10 },
      ],
    });
    expect(results).toHaveLength(3);
    expect(results[0].name).toBe("pages_1-3.pdf");
    expect(results[1].name).toBe("pages_4-6.pdf");
    expect(results[2].name).toBe("pages_7-10.pdf");

    const doc0 = await PDFDocument.load(results[0].bytes);
    const doc1 = await PDFDocument.load(results[1].bytes);
    const doc2 = await PDFDocument.load(results[2].bytes);
    expect(doc0.getPageCount()).toBe(3);
    expect(doc1.getPageCount()).toBe(3);
    expect(doc2.getPageCount()).toBe(4);
  });

  it("handles single-page range", async () => {
    const pdf = await makePdf(5);
    const results = await splitPdf(pdf, {
      mode: "by-range",
      ranges: [{ from: 3, to: 3 }],
    });
    expect(results).toHaveLength(1);
    expect(results[0].name).toBe("pages_3-3.pdf");
    const doc = await PDFDocument.load(results[0].bytes);
    expect(doc.getPageCount()).toBe(1);
  });

  it("skips invalid ranges where from > to", async () => {
    const pdf = await makePdf(5);
    const results = await splitPdf(pdf, {
      mode: "by-range",
      ranges: [{ from: 5, to: 3 }],
    });
    expect(results).toHaveLength(0);
  });
});

describe("splitPdf — select-pages mode", () => {
  it("splits selected pages into one group", async () => {
    const pdf = await makePdf(10);
    const results = await splitPdf(pdf, {
      mode: "select-pages",
      groups: [[0, 2, 4]], // 0-indexed: pages 1, 3, 5
    });
    expect(results).toHaveLength(1);
    expect(results[0].name).toBe("pages_1-5.pdf");
    const doc = await PDFDocument.load(results[0].bytes);
    expect(doc.getPageCount()).toBe(3);
  });

  it("splits into multiple groups", async () => {
    const pdf = await makePdf(10);
    const results = await splitPdf(pdf, {
      mode: "select-pages",
      groups: [
        [0, 1],
        [5, 6, 7],
      ],
    });
    expect(results).toHaveLength(2);
    expect(results[0].name).toBe("pages_1-2.pdf");
    expect(results[1].name).toBe("pages_6-8.pdf");

    const doc0 = await PDFDocument.load(results[0].bytes);
    const doc1 = await PDFDocument.load(results[1].bytes);
    expect(doc0.getPageCount()).toBe(2);
    expect(doc1.getPageCount()).toBe(3);
  });

  it("handles single page selection", async () => {
    const pdf = await makePdf(5);
    const results = await splitPdf(pdf, {
      mode: "select-pages",
      groups: [[2]], // page 3
    });
    expect(results).toHaveLength(1);
    expect(results[0].name).toBe("page_3.pdf");
  });

  it("skips empty groups", async () => {
    const pdf = await makePdf(5);
    const results = await splitPdf(pdf, {
      mode: "select-pages",
      groups: [[], [0, 1], []],
    });
    expect(results).toHaveLength(1);
  });
});

describe("splitPdf — error handling", () => {
  it("throws on corrupted PDF data", async () => {
    const corrupted = new ArrayBuffer(10);
    await expect(splitPdf(corrupted, { mode: "extract-all" })).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Add test include path to vitest.config.ts**

In `vitest.config.ts`, add `"libs/pdf-split/**/*.test.ts"` to the `include` array. Insert it after the existing `"libs/pdf-merge/**/*.test.ts"` entry (line 19).

The file currently has these entries on lines 5-31. Add the new entry after line 19:

```typescript
      "libs/pdf-merge/**/*.test.ts",
      "libs/pdf-split/**/*.test.ts",
```

- [ ] **Step 3: Run tests to verify they pass**

Run: `npx vitest run libs/pdf-split/__tests__/split.test.ts`

Expected: All tests pass (green). The `makePdf` helper creates valid PDFs in-memory, so all split operations should work.

- [ ] **Step 4: Commit**

```bash
git add libs/pdf-split/__tests__/split.test.ts vitest.config.ts
git commit -m "test(pdf-split): add unit tests for split logic"
```

---

### Task 4: UI Page Component

**Files:**

- Create: `app/[locale]/pdf-split/pdf-split-page.tsx`

This is the largest task. The component follows the three-component pattern from pdf-merge: `Conversion`, `Description`, `PdfSplitPage` (default export).

- [ ] **Step 1: Create the full page component**

```typescript
"use client";

import { useCallback, useEffect, useRef, useState } from "react";
import { useTranslations } from "next-intl";
import Layout from "../../../components/layout";
import { showToast } from "../../../libs/toast";
import { fromEvent } from "file-selector";
import { formatBytes } from "../../../utils/storage";
import { Button } from "../../../components/ui/button";
import { Download, RotateCw, Plus, X, Scissors } from "lucide-react";
import RelatedTools from "../../../components/related-tools";
import PrivacyBanner from "../../../components/privacy-banner";
import DescriptionSection from "../../../components/description-section";
import { splitPdf, getPdfPageCount } from "../../../libs/pdf-split/split";
import type { SplitMode, SplitOptions, SplitResult, SplitProgress } from "../../../libs/pdf-split/split";
import { renderPageThumbnail } from "../../../libs/pdf-split/thumbnail";
import { zipSync } from "fflate";

const THUMBNAIL_CONCURRENCY = 3;

function downloadFile(file: SplitResult) {
  const blob = new Blob([file.bytes], { type: "application/pdf" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = file.name;
  a.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}

function downloadAsZip(files: SplitResult[]) {
  const zipData: Record<string, Uint8Array> = {};
  for (const file of files) {
    zipData[file.name] = file.bytes;
  }
  const zipBytes = zipSync(zipData);
  const blob = new Blob([zipBytes], { type: "application/zip" });
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = "split-pages.zip";
  a.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}

async function renderThumbnails(
  sourceData: ArrayBuffer,
  pageCount: number,
  onThumbnail: (index: number, url: string) => void
) {
  let running = 0;
  let idx = 0;

  await new Promise<void>((resolve) => {
    function next() {
      while (running < THUMBNAIL_CONCURRENCY && idx < pageCount) {
        const currentIdx = idx;
        idx++;
        running++;

        (async () => {
          try {
            const url = await renderPageThumbnail(sourceData, currentIdx);
            onThumbnail(currentIdx, url);
          } catch {
            // thumbnail rendering failed for this page
          }

          running--;
          if (running === 0 && idx >= pageCount) resolve();
          else next();
        })();
      }
      if (running === 0 && idx >= pageCount) resolve();
    }
    next();
  });
}

function Conversion() {
  const t = useTranslations("pdf-split");
  const tc = useTranslations("common");

  // Source file
  const [sourceData, setSourceData] = useState<ArrayBuffer | null>(null);
  const [pageCount, setPageCount] = useState(0);

  // Thumbnails (pageIndex → data URL)
  const [thumbnails, setThumbnails] = useState<Map<number, string>>(new Map());

  // Mode & params
  const [mode, setMode] = useState<SplitMode>("extract-all");
  const [ranges, setRanges] = useState<{ from: number; to: number }[]>([
    { from: 1, to: 1 },
  ]);
  const [selectedPages, setSelectedPages] = useState<Set<number>>(new Set());
  const [groups, setGroups] = useState<number[][]>([[]]);

  // Processing
  const [processing, setProcessing] = useState(false);
  const [progress, setProgress] = useState<SplitProgress | null>(null);
  const [results, setResults] = useState<SplitResult[]>([]);
  const [error, setError] = useState<string | null>(null);

  const dropZoneRef = useRef<HTMLDivElement>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);

  // Refs for cleanup on unmount
  const sourceDataRef = useRef<ArrayBuffer | null>(null);
  const thumbnailsRef = useRef<Map<number, string>>(new Map());

  useEffect(() => {
    sourceDataRef.current = sourceData;
  }, [sourceData]);
  useEffect(() => {
    thumbnailsRef.current = thumbnails;
  }, [thumbnails]);
  useEffect(() => {
    return () => {
      sourceDataRef.current = null;
      thumbnailsRef.current.clear();
    };
  }, []);

  // Drag-and-drop file handling
  useEffect(() => {
    const dropZone = dropZoneRef.current;
    if (!dropZone) return;

    const onDragOver = (e: DragEvent) => {
      e.preventDefault();
      e.stopPropagation();
    };
    const onDrop = async (e: DragEvent) => {
      e.preventDefault();
      e.stopPropagation();
      const dropped = await fromEvent(e);
      if (!dropped || dropped.length === 0) return;
      const pdfFiles = (dropped as File[]).filter((f) =>
        f.name.toLowerCase().endsWith(".pdf")
      );
      if (pdfFiles.length === 0) {
        showToast(t("onlyPdfSupported"), "warning");
        return;
      }
      await loadPdf(pdfFiles[0]);
    };

    dropZone.addEventListener("dragover", onDragOver);
    dropZone.addEventListener("drop", onDrop);
    return () => {
      dropZone.removeEventListener("dragover", onDragOver);
      dropZone.removeEventListener("drop", onDrop);
    };
  }, [t]);

  async function loadPdf(file: File) {
    try {
      const data = await file.arrayBuffer();
      const count = await getPdfPageCount(data);
      setSourceData(data);
      setPageCount(count);
      setMode("extract-all");
      setRanges([{ from: 1, to: count }]);
      setSelectedPages(new Set());
      setGroups([[]]);
      setResults([]);
      setError(null);
      setThumbnails(new Map());

      // Render thumbnails with bounded concurrency
      await renderThumbnails(data, count, (index, url) => {
        setThumbnails((prev) => {
          const next = new Map(prev);
          next.set(index, url);
          return next;
        });
      });
    } catch {
      showToast(t("corruptedPdf"), "danger");
    }
  }

  async function handleFileInput(fileList: FileList | null) {
    if (!fileList || fileList.length === 0) return;
    const file = fileList[0];
    if (!file.name.toLowerCase().endsWith(".pdf")) {
      showToast(t("onlyPdfSupported"), "warning");
      return;
    }
    await loadPdf(file);
  }

  // Range helpers
  function addRange() {
    setRanges((prev) => [...prev, { from: 1, to: pageCount }]);
  }

  function removeRange(index: number) {
    setRanges((prev) => prev.filter((_, i) => i !== index));
  }

  function updateRange(index: number, field: "from" | "to", value: string) {
    setRanges((prev) => {
      const updated = [...prev];
      updated[index] = { ...updated[index], [field]: parseInt(value, 10) || 1 };
      return updated;
    });
  }

  // Select pages helpers
  function togglePage(pageIndex: number) {
    setSelectedPages((prev) => {
      const next = new Set(prev);
      if (next.has(pageIndex)) {
        next.delete(pageIndex);
      } else {
        next.add(pageIndex);
      }
      return next;
    });
    // Auto-sync to groups: group 0 = all selected pages
    setGroups((prevGroups) => {
      const selected = new Set(selectedPages);
      if (selected.has(pageIndex)) {
        selected.delete(pageIndex);
      } else {
        selected.add(pageIndex);
      }
      const newGroups = [...prevGroups];
      newGroups[0] = Array.from(selected).sort((a, b) => a - b);
      return newGroups;
    });
  }

  function selectAll() {
    const all = Array.from({ length: pageCount }, (_, i) => i);
    setSelectedPages(new Set(all));
    setGroups((prev) => {
      const updated = [...prev];
      updated[0] = all;
      return updated;
    });
  }

  function deselectAll() {
    setSelectedPages(new Set());
    setGroups((prev) => {
      const updated = [...prev];
      updated[0] = [];
      return updated;
    });
  }

  function addGroup() {
    setGroups((prev) => [...prev, []]);
  }

  function removeGroup(index: number) {
    if (groups.length <= 1) return;
    setGroups((prev) => prev.filter((_, i) => i !== index));
  }

  function assignPageToGroup(pageIndex: number, groupIndex: number) {
    setGroups((prev) => {
      const updated = prev.map((g) => g.filter((p) => p !== pageIndex));
      updated[groupIndex] = [...updated[groupIndex], pageIndex].sort((a, b) => a - b);
      return updated;
    });
    setSelectedPages((prev) => {
      const next = new Set(prev);
      next.add(pageIndex);
      return next;
    });
  }

  // Build split options from current state
  function buildSplitOptions(): SplitOptions | null {
    if (mode === "extract-all") {
      return { mode: "extract-all" };
    }
    if (mode === "by-range") {
      const validRanges = ranges.filter(
        (r) => r.from >= 1 && r.to >= r.from && r.from <= pageCount
      );
      if (validRanges.length === 0) return null;
      return { mode: "by-range", ranges: validRanges };
    }
    if (mode === "select-pages") {
      const nonEmptyGroups = groups.filter((g) => g.length > 0);
      if (nonEmptyGroups.length === 0) return null;
      return { mode: "select-pages", groups: nonEmptyGroups };
    }
    return null;
  }

  async function handleSplit() {
    if (!sourceData) return;
    const options = buildSplitOptions();
    if (!options) return;

    setProcessing(true);
    setProgress(null);
    setError(null);
    setResults([]);

    try {
      const splitResults = await splitPdf(sourceData, options, (p) => {
        setProgress(p);
      });
      setResults(splitResults);
    } catch {
      setError(t("splitFailed"));
      showToast(t("splitFailed"), "danger");
    } finally {
      setProcessing(false);
      setProgress(null);
    }
  }

  function handleNewSplit() {
    setSourceData(null);
    setPageCount(0);
    setThumbnails(new Map());
    setMode("extract-all");
    setRanges([{ from: 1, to: 1 }]);
    setSelectedPages(new Set());
    setGroups([[]]);
    setResults([]);
    setError(null);
    setProgress(null);
    setProcessing(false);
    if (fileInputRef.current) fileInputRef.current.value = "";
  }

  const canSplit =
    !processing &&
    sourceData !== null &&
    pageCount > 0 &&
    buildSplitOptions() !== null;

  // --- Result state ---
  if (results.length > 0) {
    const totalPagesInResults = results.reduce(
      (sum, r) => sum + (r.bytes.byteLength > 0 ? 1 : 0),
      0
    );
    return (
      <section className="mt-4">
        <div className="rounded-xl border border-accent-cyan/30 bg-accent-cyan-dim/10 p-6 text-center">
          <div className="text-2xl mb-2">✅</div>
          <p className="text-fg-primary font-semibold text-lg mb-2">{t("splitSuccess")}</p>
          <p className="text-fg-secondary text-sm mb-6">
            {t("totalFiles", { count: results.length })} — {t("pagesCount", { count: pageCount })}
          </p>

          <div className="space-y-2 max-w-md mx-auto text-left">
            {results.map((result, i) => (
              <div
                key={i}
                className="flex items-center justify-between gap-3 rounded-lg border border-border-default bg-bg-surface p-3"
              >
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-medium text-fg-primary truncate">{result.name}</p>
                  <p className="text-xs text-fg-muted">{formatBytes(result.bytes.byteLength)}</p>
                </div>
                <Button
                  variant="outline"
                  size="sm"
                  onClick={() => downloadFile(result)}
                >
                  <Download size={14} className="me-1" />
                  {t("download")}
                </Button>
              </div>
            ))}
          </div>

          <div className="flex items-center justify-center gap-3 mt-6">
            <Button variant="primary" size="md" onClick={() => downloadAsZip(results)}>
              <Download size={16} className="me-1.5" />
              {t("downloadZip")}
            </Button>
            <Button variant="outline" size="md" onClick={handleNewSplit}>
              <RotateCw size={16} className="me-1.5" />
              {t("newSplit")}
            </Button>
          </div>
        </div>
      </section>
    );
  }

  // --- Empty state — drop zone ---
  if (!sourceData) {
    return (
      <section className="mt-4">
        <div
          ref={dropZoneRef}
          className="relative text-xl rounded-lg border-2 border-dashed border-accent-cyan/30 bg-accent-cyan-dim/10 text-accent-cyan"
          style={{ width: "100%", height: "12rem" }}
        >
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 flex flex-col items-center justify-center px-4 pointer-events-none">
            <span className="text-3xl mb-2">📑</span>
            <span className="font-bold">{t("dropPdf")}</span>
            <span className="text-sm mt-1 text-accent-cyan/70">{t("supportedFormats")}</span>
          </div>
          <input
            ref={fileInputRef}
            type="file"
            accept=".pdf"
            className="opacity-0 absolute inset-0 w-full h-full cursor-pointer"
            onClick={() => {
              if (fileInputRef.current) fileInputRef.current.value = "";
            }}
            onChange={(e) => handleFileInput(e.target.files)}
          />
        </div>
      </section>
    );
  }

  // --- Loaded state — controls + thumbnails ---
  return (
    <section className="mt-4">
      {/* Mode tabs */}
      <div className="flex gap-2 mb-4">
        {(["extract-all", "by-range", "select-pages"] as SplitMode[]).map((m) => (
          <button
            key={m}
            type="button"
            onClick={() => setMode(m)}
            className={`px-4 py-2 rounded-lg text-sm font-medium transition-colors ${
              mode === m
                ? "bg-accent-cyan text-bg-base"
                : "bg-bg-surface text-fg-secondary hover:text-fg-primary border border-border-default"
            }`}
          >
            {t(
              m === "extract-all"
                ? "modeExtractAll"
                : m === "by-range"
                  ? "modeByRange"
                  : "modeSelectPages"
            )}
          </button>
        ))}
      </div>

      {/* Thumbnail grid */}
      <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-3 mb-4">
        {Array.from({ length: pageCount }, (_, i) => {
          const isSelected = selectedPages.has(i);
          const inRange =
            mode === "by-range" &&
            ranges.some((r) => i >= r.from - 1 && i <= r.to - 1);

          return (
            <div
              key={i}
              onClick={() => {
                if (mode === "select-pages") togglePage(i);
              }}
              className={`relative rounded-lg border overflow-hidden cursor-pointer transition-all ${
                isSelected || inRange
                  ? "border-accent-cyan ring-2 ring-accent-cyan/30"
                  : "border-border-default hover:border-accent-cyan/50"
              } ${mode === "select-pages" ? "" : "cursor-default"}`}
            >
              <div className="aspect-[3/4] bg-bg-input flex items-center justify-center">
                {thumbnails.has(i) ? (
                  /* eslint-disable-next-line @next/next/no-img-element -- data URL thumbnail */
                  <img
                    src={thumbnails.get(i)!}
                    alt={t("page", { num: i + 1 })}
                    className="w-full h-full object-contain"
                  />
                ) : (
                  <div className="w-5 h-5 border-2 border-accent-cyan border-t-transparent rounded-full animate-spin" />
                )}
              </div>
              <div className="px-2 py-1 text-xs text-fg-muted text-center truncate">
                {i + 1}
              </div>
              {mode === "select-pages" && isSelected && (
                <div className="absolute top-1 right-1 w-5 h-5 rounded-full bg-accent-cyan text-bg-base flex items-center justify-center text-xs font-bold">
                  ✓
                </div>
              )}
            </div>
          );
        })}
      </div>

      <p className="text-sm text-fg-secondary mb-4">
        {t("pagesCount", { count: pageCount })}
      </p>

      {/* Mode-specific controls */}
      {mode === "by-range" && (
        <div className="space-y-2 mb-4">
          {ranges.map((range, idx) => (
            <div key={idx} className="flex items-center gap-2">
              <span className="text-sm text-fg-secondary">{t("rangeFrom")}</span>
              <input
                type="number"
                min={1}
                max={pageCount}
                value={range.from}
                onChange={(e) => updateRange(idx, "from", e.target.value)}
                className="w-20 px-2 py-1 text-sm rounded border border-border-default bg-bg-input text-fg-primary"
              />
              <span className="text-sm text-fg-secondary">{t("rangeTo")}</span>
              <input
                type="number"
                min={1}
                max={pageCount}
                value={range.to}
                onChange={(e) => updateRange(idx, "to", e.target.value)}
                className="w-20 px-2 py-1 text-sm rounded border border-border-default bg-bg-input text-fg-primary"
              />
              {ranges.length > 1 && (
                <button
                  type="button"
                  onClick={() => removeRange(idx)}
                  className="text-fg-muted hover:text-danger transition-colors"
                  title={t("removeRange")}
                >
                  <X size={16} />
                </button>
              )}
            </div>
          ))}
          <Button variant="outline" size="sm" onClick={addRange}>
            <Plus size={14} className="me-1" />
            {t("addRange")}
          </Button>
        </div>
      )}

      {mode === "select-pages" && (
        <div className="flex gap-2 mb-4">
          <Button variant="outline" size="sm" onClick={selectAll}>
            {t("selectAll")}
          </Button>
          <Button variant="outline" size="sm" onClick={deselectAll}>
            {t("deselectAll")}
          </Button>
          <Button variant="outline" size="sm" onClick={addGroup}>
            <Plus size={14} className="me-1" />
            {t("newGroup")}
          </Button>
        </div>
      )}

      {/* Progress bar */}
      {processing && progress && (
        <div className="mt-4">
          <div className="flex items-center justify-between text-xs text-fg-muted mb-1">
            <span>
              {t("splitProgress", { current: progress.current, total: progress.total })}
            </span>
            <span>{Math.round((progress.current / progress.total) * 100)}%</span>
          </div>
          <div className="w-full h-2 rounded-full bg-bg-input overflow-hidden">
            <div
              className="h-full bg-accent-cyan rounded-full transition-all duration-300"
              style={{ width: `${(progress.current / progress.total) * 100}%` }}
            />
          </div>
        </div>
      )}

      {/* Split button */}
      <div className="mt-4 flex justify-center">
        <Button
          variant="primary"
          size="lg"
          disabled={!canSplit}
          onClick={handleSplit}
          className="w-full max-w-md rounded-full uppercase font-bold"
        >
          <Scissors size={18} className="me-1.5" />
          {processing ? t("splitting") : t("splitButton")}
        </Button>
      </div>

      {error && <p className="mt-2 text-sm text-danger">{error}</p>}
    </section>
  );
}

function Description() {
  return <DescriptionSection namespace="pdf-split" />;
}

export default function PdfSplitPage() {
  const t = useTranslations("tools");
  const title = t("pdf-split.shortTitle");
  return (
    <Layout title={title} categoryLabel={t("categories.visual")} categorySlug="visual-media">
      <div className="container mx-auto px-4 pt-3 pb-6">
        <PrivacyBanner variant="files" />
        <Conversion />
        <Description />
        <RelatedTools currentTool="pdf-split" />
      </div>
    </Layout>
  );
}
```

- [ ] **Step 2: Verify no TypeScript errors**

This step will be done as part of Task 8 (final verification). The file depends on the i18n keys from Task 6 and the tool registration from Task 5.

- [ ] **Step 3: Commit**

```bash
git add app/\[locale\]/pdf-split/pdf-split-page.tsx
git commit -m "feat(pdf-split): add UI page component with three split modes"
```

---

### Task 5: Tool Registration

**Files:**

- Modify: `libs/tools.ts`

- [ ] **Step 1: Add Scissors icon import**

In `libs/tools.ts`, add `Scissors` to the lucide-react import block. The import is on lines 3-49. Add `Scissors` after `Layers` (line 46) and before `FileStack` (line 47):

```typescript
  Layers,
  Scissors,
  FileStack,
```

- [ ] **Step 2: Add pdf-split TOOLS entry**

In the `TOOLS` array, add a new entry after the `pdf-merge` entry (after line 499, before line 500). The pdf-merge entry ends at line 499 with `},`.

```typescript
  {
    key: "pdf-split",
    path: "/pdf-split",
    icon: Scissors,
    emoji: "✂️",
    sameAs: [
      "https://www.adobe.com/acrobat/online/split-pdf.html",
    ],
  },
```

- [ ] **Step 3: Add pdf-split to visual TOOL_CATEGORIES**

In the `TOOL_CATEGORIES` array, the `visual` category is at lines 126-138. Add `"pdf-split"` after `"pdf-merge"` on line 136:

```typescript
  {
    key: "visual",
    tools: [
      "color",
      "image-resize",
      "image-compress",
      "image-convert",
      "image-watermark",
      "image-crop",
      "image-rotate",
      "pdf-merge",
      "pdf-split",
    ],
  },
```

- [ ] **Step 4: Add TOOL_RELATIONS entries**

In the `TOOL_RELATIONS` object (lines 148-242), add `"pdf-split"` relations and update `"pdf-merge"` relations.

Add after line 241 (`"pdf-merge": ["image-compress", "image-convert", "checksum"],`):

```typescript
  "pdf-split": ["pdf-merge", "image-compress", "image-convert"],
```

Update the existing `"pdf-merge"` entry on line 241 to include `"pdf-split"`:

```typescript
  "pdf-merge": ["image-compress", "image-convert", "checksum", "pdf-split"],
```

- [ ] **Step 5: Commit**

```bash
git add libs/tools.ts
git commit -m "feat(pdf-split): register tool in TOOLS, categories, and relations"
```

---

### Task 6: English i18n (Source of Truth)

**Files:**

- Modify: `public/locales/en/tools.json` (add pdf-split entry)
- Create: `public/locales/en/pdf-split.json`

- [ ] **Step 1: Add pdf-split entry to `public/locales/en/tools.json`**

Add this entry alongside the existing `pdf-merge` entry:

```json
"pdf-split": {
  "title": "PDF Split - Split PDF into Pages or Ranges",
  "shortTitle": "PDF Split",
  "description": "Split a PDF into separate pages, custom ranges, or hand-picked selections. Thumbnail preview, ZIP download. All processing runs in your browser."
}
```

Note: English locale omits `searchTerms` (fuzzysort matches `shortTitle` directly).

- [ ] **Step 2: Create `public/locales/en/pdf-split.json`**

Keys are flat (not nested in a `"ui"` object), matching pdf-merge's pattern. The `descriptions` key is the only nested object.

```json
{
  "dropPdf": "Drop a PDF file here or click to upload",
  "supportedFormats": "Supports PDF files only",
  "onlyPdfSupported": "Only PDF files are supported",
  "modeExtractAll": "Extract Every Page",
  "modeByRange": "Split by Range",
  "modeSelectPages": "Select Pages",
  "rangeFrom": "From",
  "rangeTo": "To",
  "addRange": "Add Range",
  "removeRange": "Remove",
  "newGroup": "New Group",
  "selectAll": "Select All",
  "deselectAll": "Deselect All",
  "splitButton": "Split PDF",
  "splitting": "Splitting...",
  "splitProgress": "Processing page {current} of {total}...",
  "splitSuccess": "Split successfully!",
  "downloadZip": "Download All as ZIP",
  "download": "Download",
  "newSplit": "New Split",
  "page": "Page {num}",
  "pages": "Pages {from}-{to}",
  "pagesCount": "{count} pages",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} files generated",
  "processing": "Splitting...",
  "invalidRange": "Page range must be between 1 and {max}",
  "overlapRange": "Page ranges overlap — pages in multiple ranges will appear in each output",
  "encryptedPdf": "This PDF is encrypted and may not split correctly",
  "corruptedPdf": "This PDF file is corrupted and cannot be read",
  "splitFailed": "Split failed. Please check your PDF file and try again.",
  "descriptions": {
    "title": "About PDF Splitter",
    "aeoDefinition": "PDF Splitter is a free online tool for splitting PDF files into separate pages, custom page ranges, or hand-picked selections. Preview thumbnails, download individually or as ZIP. All processing runs locally in your browser.",
    "whatIsTitle": "What is the PDF Splitter?",
    "whatIs": "Split a PDF file into separate pages or custom page ranges directly in your browser. Upload your PDF, choose a split mode (extract every page, split by ranges, or select specific pages), preview thumbnails, and download the results. No data is uploaded to any server — all processing uses the pdf-lib library.",
    "useCases": "When to use PDF Splitter",
    "useCasesP1": "Extract specific pages from a long report or contract to share only the relevant sections.",
    "useCasesP2": "Split a multi-page PDF into individual pages for separate processing or distribution.",
    "stepsTitle": "How to Split a PDF",
    "step1Title": "Upload your PDF",
    "step1Text": "Drag and drop your PDF file or click to select it from your computer.",
    "step2Title": "Choose split mode",
    "step2Text": "Select how to split: extract every page, define custom ranges, or click thumbnails to pick specific pages.",
    "step3Title": "Split and download",
    "step3Text": "Click the Split button to generate the output files. Download individually or as a ZIP archive.",
    "faq1Q": "Is my PDF data sent to a server?",
    "faq1A": "No. All processing happens entirely in your browser. Your PDF files never leave your device.",
    "faq2Q": "What PDF features are preserved after splitting?",
    "faq2A": "Text, images, fonts, annotations, and form fields are preserved. Some advanced features like JavaScript actions may not carry over.",
    "faq3Q": "Is there a file size limit?",
    "faq3A": "There is no strict limit, but very large files (hundreds of pages) may take longer to process. The tool handles files of any size your browser can hold in memory."
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add public/locales/en/tools.json public/locales/en/pdf-split.json
git commit -m "feat(pdf-split): add English i18n source of truth"
```

---

### Task 7: Non-English i18n (9 Locales)

**Files:**

- Modify: `public/locales/{zh-CN,zh-TW,ja,ko,es,pt-BR,fr,de,ru}/tools.json` (add pdf-split entry)
- Create: `public/locales/{zh-CN,zh-TW,ja,ko,es,pt-BR,fr,de,ru}/pdf-split.json`

This task creates all 9 non-English locale files. Each needs two changes: add entry to `tools.json` and create `pdf-split.json`.

- [ ] **Step 1: zh-CN — Add to tools.json**

Add this entry to `public/locales/zh-CN/tools.json`:

```json
"pdf-split": {
  "title": "PDF 拆分工具 - 在线拆分 PDF 页面或范围",
  "shortTitle": "PDF 拆分",
  "description": "将 PDF 拆分为单独页面、自定义范围或手动选择。缩略图预览、ZIP 下载。所有处理在浏览器本地完成。",
  "searchTerms": "pdfchaifen pdfcf chaifen fenye chaiye"
}
```

- [ ] **Step 2: zh-CN — Create pdf-split.json**

Create `public/locales/zh-CN/pdf-split.json`:

```json
{
  "dropPdf": "拖放 PDF 文件到此处，或点击上传",
  "supportedFormats": "仅支持 PDF 文件",
  "onlyPdfSupported": "仅支持 PDF 文件",
  "modeExtractAll": "逐页提取",
  "modeByRange": "按范围拆分",
  "modeSelectPages": "选择页面",
  "rangeFrom": "从",
  "rangeTo": "到",
  "addRange": "添加范围",
  "removeRange": "移除",
  "newGroup": "新建分组",
  "selectAll": "全选",
  "deselectAll": "取消全选",
  "splitButton": "拆分 PDF",
  "splitting": "拆分中...",
  "splitProgress": "正在处理第 {current} 页（共 {total} 页）...",
  "splitSuccess": "拆分成功！",
  "downloadZip": "全部下载为 ZIP",
  "download": "下载",
  "newSplit": "重新拆分",
  "page": "第 {num} 页",
  "pages": "第 {from}-{to} 页",
  "pagesCount": "{count} 页",
  "splitResult": "{name} — {size}",
  "totalFiles": "生成了 {count} 个文件",
  "processing": "拆分中...",
  "invalidRange": "页码范围必须在 1 到 {max} 之间",
  "overlapRange": "页码范围重叠 — 重复页面将出现在每个输出文件中",
  "encryptedPdf": "此 PDF 已加密，可能无法正确拆分",
  "corruptedPdf": "此 PDF 文件已损坏，无法读取",
  "splitFailed": "拆分失败，请检查 PDF 文件后重试。",
  "descriptions": {
    "title": "关于 PDF 拆分工具",
    "aeoDefinition": "PDF 拆分工具是一款免费的在线工具，可将 PDF 拆分为单独页面、自定义范围或手动选择。缩略图预览、单独或批量下载。所有处理均在浏览器本地完成。",
    "whatIsTitle": "什么是 PDF 拆分工具？",
    "whatIs": "在浏览器中将 PDF 拆分为单独页面或自定义范围。上传 PDF，选择拆分模式（逐页提取、按范围拆分或手动选择页面），预览缩略图，下载结果。不会上传任何数据到服务器——所有处理均使用 pdf-lib 库完成。",
    "useCases": "何时使用 PDF 拆分工具",
    "useCasesP1": "从长篇报告或合同中提取特定页面，仅分享相关内容。",
    "useCasesP2": "将多页 PDF 拆分为单独页面，便于分别处理或分发。",
    "stepsTitle": "如何拆分 PDF",
    "step1Title": "上传 PDF",
    "step1Text": "拖放 PDF 文件到上传区域，或点击从电脑中选择。",
    "step2Title": "选择拆分模式",
    "step2Text": "选择拆分方式：逐页提取、自定义范围或点击缩略图选择特定页面。",
    "step3Title": "拆分并下载",
    "step3Text": "点击拆分按钮生成输出文件。可单独下载或打包为 ZIP 下载。",
    "faq1Q": "我的 PDF 数据会上传到服务器吗？",
    "faq1A": "不会。所有处理均在浏览器中本地完成。您的 PDF 文件不会离开您的设备。",
    "faq2Q": "拆分后 PDF 的哪些特性会被保留？",
    "faq2A": "文本、图片、字体、注释和表单字段都会保留。部分高级特性如 JavaScript 动作可能不会保留。",
    "faq3Q": "有文件大小限制吗？",
    "faq3A": "没有严格限制，但非常大的文件（数百页）可能需要更长处理时间。工具可以处理浏览器内存允许的任何大小的文件。"
  }
}
```

- [ ] **Step 3: zh-TW — Add to tools.json**

```json
"pdf-split": {
  "title": "PDF 拆分工具 - 線上拆分 PDF 頁面或範圍",
  "shortTitle": "PDF 拆分",
  "description": "將 PDF 拆分為單獨頁面、自訂範圍或手動選擇。縮圖預覽、ZIP 下載。所有處理在瀏覽器本機完成。",
  "searchTerms": "pdfchaifen pdfcf chafen fenye"
}
```

- [ ] **Step 4: zh-TW — Create pdf-split.json**

Create `public/locales/zh-TW/pdf-split.json`:

```json
{
  "dropPdf": "拖放 PDF 檔案到此处，或點選上傳",
  "supportedFormats": "僅支援 PDF 檔案",
  "onlyPdfSupported": "僅支援 PDF 檔案",
  "modeExtractAll": "逐頁提取",
  "modeByRange": "按範圍拆分",
  "modeSelectPages": "選擇頁面",
  "rangeFrom": "從",
  "rangeTo": "到",
  "addRange": "新增範圍",
  "removeRange": "移除",
  "newGroup": "新增分組",
  "selectAll": "全選",
  "deselectAll": "取消全選",
  "splitButton": "拆分 PDF",
  "splitting": "拆分中...",
  "splitProgress": "正在處理第 {current} 頁（共 {total} 頁）...",
  "splitSuccess": "拆分成功！",
  "downloadZip": "全部下載為 ZIP",
  "download": "下載",
  "newSplit": "重新拆分",
  "page": "第 {num} 頁",
  "pages": "第 {from}-{to} 頁",
  "pagesCount": "{count} 頁",
  "splitResult": "{name} — {size}",
  "totalFiles": "產生了 {count} 個檔案",
  "processing": "拆分中...",
  "invalidRange": "頁碼範圍必須在 1 到 {max} 之間",
  "overlapRange": "頁碼範圍重疊 — 重複頁面將出現在每個輸出檔案中",
  "encryptedPdf": "此 PDF 已加密，可能無法正確拆分",
  "corruptedPdf": "此 PDF 檔案已損毀，無法讀取",
  "splitFailed": "拆分失敗，請檢查 PDF 檔案後重試。",
  "descriptions": {
    "title": "關於 PDF 拆分工具",
    "aeoDefinition": "PDF 拆分工具是一款免費的線上工具，可將 PDF 拆分為單獨頁面、自訂範圍或手動選擇。縮圖預覽、單獨或批次下載。所有處理均在瀏覽器本機完成。",
    "whatIsTitle": "什麼是 PDF 拆分工具？",
    "whatIs": "在瀏覽器中將 PDF 拆分為單獨頁面或自訂範圍。上傳 PDF，選擇拆分模式（逐頁提取、按範圍拆分或手動選擇頁面），預覽縮圖，下載結果。不會上傳任何資料到伺服器——所有處理均使用 pdf-lib 函式庫完成。",
    "useCases": "何時使用 PDF 拆分工具",
    "useCasesP1": "從長篇報告或合約中提取特定頁面，僅分享相關內容。",
    "useCasesP2": "將多頁 PDF 拆分為單獨頁面，便於分別處理或分發。",
    "stepsTitle": "如何拆分 PDF",
    "step1Title": "上傳 PDF",
    "step1Text": "拖放 PDF 檔案到上傳區域，或點選從電腦中選擇。",
    "step2Title": "選擇拆分模式",
    "step2Text": "選擇拆分方式：逐頁提取、自訂範圍或點選縮圖選擇特定頁面。",
    "step3Title": "拆分並下載",
    "step3Text": "點選拆分按鈕產生輸出檔案。可單獨下載或打包為 ZIP 下載。",
    "faq1Q": "我的 PDF 資料會上傳到伺服器嗎？",
    "faq1A": "不會。所有處理均在瀏覽器中本機完成。您的 PDF 檔案不會離開您的裝置。",
    "faq2Q": "拆分後 PDF 的哪些特性會被保留？",
    "faq2A": "文字、圖片、字型、註釋和表單欄位都會保留。部分進階特性如 JavaScript 動作可能不會保留。",
    "faq3Q": "有檔案大小限制嗎？",
    "faq3A": "沒有嚴格限制，但非常大的檔案（數百頁）可能需要更長處理時間。工具可以處理瀏覽器記憶體允許的任何大小的檔案。"
  }
}
```

- [ ] **Step 5: ja — Add to tools.json**

```json
"pdf-split": {
  "title": "PDF 分割ツール - PDF をページまたは範囲ごとに分割",
  "shortTitle": "PDF 分割",
  "description": "PDF を個別ページ、カスタム範囲、または手動選択で分割。サムネイルプレビュー、ZIP ダウンロード。すべての処理はブラウザで実行されます。",
  "searchTerms": "pdfbunkatsu pdfbs bunkatsu pdf"
}
```

- [ ] **Step 6: ja — Create pdf-split.json**

Create `public/locales/ja/pdf-split.json`:

```json
{
  "dropPdf": "PDF ファイルをここにドロップ、またはクリックしてアップロード",
  "supportedFormats": "PDF ファイルのみ対応",
  "onlyPdfSupported": "PDF ファイルのみ対応しています",
  "modeExtractAll": "全ページ抽出",
  "modeByRange": "範囲で分割",
  "modeSelectPages": "ページ選択",
  "rangeFrom": "開始",
  "rangeTo": "終了",
  "addRange": "範囲を追加",
  "removeRange": "削除",
  "newGroup": "新しいグループ",
  "selectAll": "すべて選択",
  "deselectAll": "選択解除",
  "splitButton": "PDF を分割",
  "splitting": "分割中...",
  "splitProgress": "{total} ページ中 {current} ページ目を処理中...",
  "splitSuccess": "分割完了！",
  "downloadZip": "ZIP で一括ダウンロード",
  "download": "ダウンロード",
  "newSplit": "新しく分割",
  "page": "{num} ページ",
  "pages": "{from}-{to} ページ",
  "pagesCount": "{count} ページ",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} ファイル生成",
  "processing": "分割中...",
  "invalidRange": "ページ範囲は 1 から {max} の間で指定してください",
  "overlapRange": "ページ範囲が重複しています — 重複ページは各出力に含まれます",
  "encryptedPdf": "この PDF は暗号化されており、正しく分割できない場合があります",
  "corruptedPdf": "この PDF ファイルは破損しており、読み取れません",
  "splitFailed": "分割に失敗しました。PDF ファイルを確認して再試行してください。",
  "descriptions": {
    "title": "PDF 分割ツールについて",
    "aeoDefinition": "PDF 分割ツールは、PDF ファイルを個別ページ、カスタム範囲、または手動選択で分割する無料のオンラインツールです。サムネイルプレビュー、個別または ZIP ダウンロード。すべての処理はブラウザ内でローカルに実行されます。",
    "whatIsTitle": "PDF 分割ツールとは？",
    "whatIs": "ブラウザ内で PDF を個別ページやカスタム範囲に分割します。PDF をアップロードし、分割モード（全ページ抽出、範囲指定、ページ選択）を選び、サムネイルをプレビューして結果をダウンロードします。データはサーバーにアップロードされません — すべての処理は pdf-lib ライブラリを使用します。",
    "useCases": "PDF 分割ツールの活用場面",
    "useCasesP1": "長いレポートや契約書から特定のページを抽出し、必要な部分だけを共有します。",
    "useCasesP2": "複数ページの PDF を個別ページに分割し、別々に処理または配信します。",
    "stepsTitle": "PDF の分割方法",
    "step1Title": "PDF をアップロード",
    "step1Text": "PDF ファイルをドロップゾーンにドラッグ＆ドロップするか、クリックして選択してください。",
    "step2Title": "分割モードを選択",
    "step2Text": "分割方法を選択：全ページ抽出、カスタム範囲、またはサムネイルをクリックして特定ページを選択。",
    "step3Title": "分割してダウンロード",
    "step3Text": "分割ボタンをクリックして出力ファイルを生成。個別または ZIP アーカイブでダウンロードできます。",
    "faq1Q": "PDF データはサーバーに送信されますか？",
    "faq1A": "いいえ。すべての処理はブラウザ内でローカルに実行されます。PDF ファイルがデバイスを離れることはありません。",
    "faq2Q": "分割後にどの PDF 機能が保持されますか？",
    "faq2A": "テキスト、画像、フォント、注釈、フォームフィールドは保持されます。JavaScript アクションなどの一部の高度な機能は引き継がれない場合があります。",
    "faq3Q": "ファイルサイズの制限はありますか？",
    "faq3A": "厳密な制限はありませんが、非常に大きなファイル（数百ページ）は処理に時間がかかる場合があります。ブラウザのメモリに収まるサイズであれば任意のファイルを処理できます。"
  }
}
```

- [ ] **Step 7: ko — Add to tools.json**

```json
"pdf-split": {
  "title": "PDF 분할 도구 - PDF를 페이지 또는 범위별로 분할",
  "shortTitle": "PDF 분할",
  "description": "PDF를 개별 페이지, 사용자 정의 범위 또는 수동 선택으로 분할합니다. 썸네일 미리보기, ZIP 다운로드. 모든 처리는 브라우저에서 실행됩니다.",
  "searchTerms": "pdfbunhal pdfbh bunhal pdf"
}
```

- [ ] **Step 8: ko — Create pdf-split.json**

Create `public/locales/ko/pdf-split.json`:

```json
{
  "dropPdf": "PDF 파일을 여기에 드롭하거나 클릭하여 업로드",
  "supportedFormats": "PDF 파일만 지원",
  "onlyPdfSupported": "PDF 파일만 지원됩니다",
  "modeExtractAll": "모든 페이지 추출",
  "modeByRange": "범위로 분할",
  "modeSelectPages": "페이지 선택",
  "rangeFrom": "시작",
  "rangeTo": "끝",
  "addRange": "범위 추가",
  "removeRange": "삭제",
  "newGroup": "새 그룹",
  "selectAll": "모두 선택",
  "deselectAll": "선택 해제",
  "splitButton": "PDF 분할",
  "splitting": "분할 중...",
  "splitProgress": "전체 {total}페이지 중 {current}페이지 처리 중...",
  "splitSuccess": "분할 완료!",
  "downloadZip": "ZIP으로 일괄 다운로드",
  "download": "다운로드",
  "newSplit": "새 분할",
  "page": "{num}페이지",
  "pages": "{from}-{to}페이지",
  "pagesCount": "{count}페이지",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count}개 파일 생성",
  "processing": "분할 중...",
  "invalidRange": "페이지 범위는 1에서 {max} 사이여야 합니다",
  "overlapRange": "페이지 범위가 겹칩니다 — 겹치는 페이지가 각 출력에 포함됩니다",
  "encryptedPdf": "이 PDF는 암호화되어 올바르게 분할되지 않을 수 있습니다",
  "corruptedPdf": "이 PDF 파일이 손상되어 읽을 수 없습니다",
  "splitFailed": "분할에 실패했습니다. PDF 파일을 확인하고 다시 시도하세요.",
  "descriptions": {
    "title": "PDF 분할 도구 소개",
    "aeoDefinition": "PDF 분할 도구는 PDF 파일을 개별 페이지, 사용자 정의 범위 또는 수동 선택으로 분할하는 무료 온라인 도구입니다. 썸네일 미리보기, 개별 또는 ZIP 다운로드. 모든 처리는 브라우저에서 로컬로 실행됩니다.",
    "whatIsTitle": "PDF 분할 도구란?",
    "whatIs": "브라우저에서 PDF를 개별 페이지나 사용자 정의 범위로 분할합니다. PDF를 업로드하고 분할 모드(모든 페이지 추출, 범위 지정, 페이지 선택)를 선택한 후 썸네일을 미리보고 결과를 다운로드합니다. 데이터는 서버에 업로드되지 않습니다 — 모든 처리는 pdf-lib 라이브러리를 사용합니다.",
    "useCases": "PDF 분할 도구 활용 시기",
    "useCasesP1": "긴 보고서나 계약서에서 특정 페이지를 추출하여 관련 부분만 공유합니다.",
    "useCasesP2": "여러 페이지 PDF를 개별 페이지로 분할하여 별도로 처리하거나 배포합니다.",
    "stepsTitle": "PDF 분할 방법",
    "step1Title": "PDF 업로드",
    "step1Text": "PDF 파일을 드롭 영역에 드래그 앤 드롭하거나 클릭하여 선택하세요.",
    "step2Title": "분할 모드 선택",
    "step2Text": "분할 방법을 선택하세요: 모든 페이지 추출, 사용자 정의 범위, 또는 썸네일을 클릭하여 특정 페이지 선택.",
    "step3Title": "분할 및 다운로드",
    "step3Text": "분할 버튼을 클릭하여 출력 파일을 생성합니다. 개별 또는 ZIP 아카이브로 다운로드할 수 있습니다.",
    "faq1Q": "PDF 데이터가 서버로 전송되나요?",
    "faq1A": "아니요. 모든 처리는 브라우저에서 로컬로 실행됩니다. PDF 파일이 기기를 떠나지 않습니다.",
    "faq2Q": "분할 후 어떤 PDF 기능이 유지되나요?",
    "faq2A": "텍스트, 이미지, 글꼴, 주석, 양식 필드는 유지됩니다. JavaScript 액션 등 일부 고급 기능은 유지되지 않을 수 있습니다.",
    "faq3Q": "파일 크기 제한이 있나요?",
    "faq3A": "엄격한 제한은 없지만, 매우 큰 파일(수백 페이지)은 처리에 더 오래 걸릴 수 있습니다. 브라우저 메모리에 수용할 수 있는 모든 크기의 파일을 처리할 수 있습니다."
  }
}
```

- [ ] **Step 9: es — Add to tools.json**

```json
"pdf-split": {
  "title": "Dividir PDF - Separar PDF por páginas o rangos",
  "shortTitle": "Dividir PDF",
  "description": "Divide un PDF en páginas individuales, rangos personalizados o selecciones manuales. Vista previa de miniaturas, descarga ZIP. Todo el procesamiento se ejecuta en tu navegador."
}
```

- [ ] **Step 10: es — Create pdf-split.json**

Create `public/locales/es/pdf-split.json`:

```json
{
  "dropPdf": "Suelta un archivo PDF aquí o haz clic para subirlo",
  "supportedFormats": "Solo se admiten archivos PDF",
  "onlyPdfSupported": "Solo se admiten archivos PDF",
  "modeExtractAll": "Extraer todas las páginas",
  "modeByRange": "Dividir por rango",
  "modeSelectPages": "Seleccionar páginas",
  "rangeFrom": "Desde",
  "rangeTo": "Hasta",
  "addRange": "Añadir rango",
  "removeRange": "Eliminar",
  "newGroup": "Nuevo grupo",
  "selectAll": "Seleccionar todo",
  "deselectAll": "Deseleccionar todo",
  "splitButton": "Dividir PDF",
  "splitting": "Dividiendo...",
  "splitProgress": "Procesando página {current} de {total}...",
  "splitSuccess": "¡Dividido con éxito!",
  "downloadZip": "Descargar todo como ZIP",
  "download": "Descargar",
  "newSplit": "Nueva división",
  "page": "Página {num}",
  "pages": "Páginas {from}-{to}",
  "pagesCount": "{count} páginas",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} archivos generados",
  "processing": "Dividiendo...",
  "invalidRange": "El rango de páginas debe estar entre 1 y {max}",
  "overlapRange": "Los rangos de páginas se superponen — las páginas en múltiples rangos aparecerán en cada salida",
  "encryptedPdf": "Este PDF está cifrado y puede no dividirse correctamente",
  "corruptedPdf": "Este archivo PDF está dañado y no se puede leer",
  "splitFailed": "Error al dividir. Comprueba tu archivo PDF e inténtalo de nuevo.",
  "descriptions": {
    "title": "Sobre Dividir PDF",
    "aeoDefinition": "Dividir PDF es una herramienta online gratuita para dividir archivos PDF en páginas individuales, rangos personalizados o selecciones manuales. Vista previa de miniaturas, descarga individual o en ZIP. Todo el procesamiento se ejecuta localmente en tu navegador.",
    "whatIsTitle": "¿Qué es Dividir PDF?",
    "whatIs": "Divide un archivo PDF en páginas individuales o rangos personalizados directamente en tu navegador. Sube tu PDF, elige un modo de división (extraer todas las páginas, dividir por rangos o seleccionar páginas específicas), previsualiza las miniaturas y descarga los resultados. No se suben datos a ningún servidor — todo el procesamiento usa la biblioteca pdf-lib.",
    "useCases": "Cuándo usar Dividir PDF",
    "useCasesP1": "Extrae páginas específicas de un informe largo o contrato para compartir solo las secciones relevantes.",
    "useCasesP2": "Divide un PDF de varias páginas en páginas individuales para procesarlas o distribuirlas por separado.",
    "stepsTitle": "Cómo dividir un PDF",
    "step1Title": "Sube tu PDF",
    "step1Text": "Arrastra y suelta tu archivo PDF o haz clic para seleccionarlo desde tu ordenador.",
    "step2Title": "Elige el modo de división",
    "step2Text": "Selecciona cómo dividir: extraer todas las páginas, definir rangos personalizados o hacer clic en las miniaturas para elegir páginas específicas.",
    "step3Title": "Divide y descarga",
    "step3Text": "Haz clic en el botón Dividir para generar los archivos de salida. Descárgalos individualmente o como archivo ZIP.",
    "faq1Q": "¿Mis datos PDF se envían a un servidor?",
    "faq1A": "No. Todo el procesamiento ocurre completamente en tu navegador. Tus archivos PDF nunca salen de tu dispositivo.",
    "faq2Q": "¿Qué características del PDF se conservan después de dividir?",
    "faq2A": "Se conservan texto, imágenes, fuentes, anotaciones y campos de formulario. Algunas características avanzadas como acciones JavaScript pueden no transferirse.",
    "faq3Q": "¿Hay un límite de tamaño de archivo?",
    "faq3A": "No hay un límite estricto, pero archivos muy grandes (cientos de páginas) pueden tardar más en procesarse. La herramienta maneja archivos de cualquier tamaño que tu navegador pueda mantener en memoria."
  }
}
```

- [ ] **Step 11: pt-BR — Add to tools.json**

```json
"pdf-split": {
  "title": "Dividir PDF - Separar PDF por Páginas ou Intervalos",
  "shortTitle": "Dividir PDF",
  "description": "Divida um PDF em páginas individuais, intervalos personalizados ou seleções manuais. Pré-visualização de miniaturas, download em ZIP. Todo o processamento é feito no seu navegador."
}
```

- [ ] **Step 12: pt-BR — Create pdf-split.json**

Create `public/locales/pt-BR/pdf-split.json`:

```json
{
  "dropPdf": "Solte um arquivo PDF aqui ou clique para enviar",
  "supportedFormats": "Somente arquivos PDF são suportados",
  "onlyPdfSupported": "Somente arquivos PDF são suportados",
  "modeExtractAll": "Extrair todas as páginas",
  "modeByRange": "Dividir por intervalo",
  "modeSelectPages": "Selecionar páginas",
  "rangeFrom": "De",
  "rangeTo": "Até",
  "addRange": "Adicionar intervalo",
  "removeRange": "Remover",
  "newGroup": "Novo grupo",
  "selectAll": "Selecionar tudo",
  "deselectAll": "Desselecionar tudo",
  "splitButton": "Dividir PDF",
  "splitting": "Dividindo...",
  "splitProgress": "Processando página {current} de {total}...",
  "splitSuccess": "Dividido com sucesso!",
  "downloadZip": "Baixar tudo como ZIP",
  "download": "Baixar",
  "newSplit": "Nova divisão",
  "page": "Página {num}",
  "pages": "Páginas {from}-{to}",
  "pagesCount": "{count} páginas",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} arquivos gerados",
  "processing": "Dividindo...",
  "invalidRange": "O intervalo de páginas deve estar entre 1 e {max}",
  "overlapRange": "Os intervalos de páginas se sobrepõem — páginas em múltiplos intervalos aparecerão em cada saída",
  "encryptedPdf": "Este PDF está criptografado e pode não ser dividido corretamente",
  "corruptedPdf": "Este arquivo PDF está corrompido e não pode ser lido",
  "splitFailed": "Falha ao dividir. Verifique seu arquivo PDF e tente novamente.",
  "descriptions": {
    "title": "Sobre Dividir PDF",
    "aeoDefinition": "Dividir PDF é uma ferramenta online gratuita para dividir arquivos PDF em páginas individuais, intervalos personalizados ou seleções manuais. Pré-visualização de miniaturas, download individual ou em ZIP. Todo o processamento é feito localmente no seu navegador.",
    "whatIsTitle": "O que é o Dividir PDF?",
    "whatIs": "Divida um arquivo PDF em páginas individuais ou intervalos personalizados diretamente no seu navegador. Faça upload do seu PDF, escolha um modo de divisão (extrair todas as páginas, dividir por intervalos ou selecionar páginas específicas), visualize as miniaturas e baixe os resultados. Nenhum dado é enviado a qualquer servidor — todo o processamento usa a biblioteca pdf-lib.",
    "useCases": "Quando usar o Dividir PDF",
    "useCasesP1": "Extraia páginas específicas de um relatório longo ou contrato para compartilhar apenas as seções relevantes.",
    "useCasesP2": "Divida um PDF de várias páginas em páginas individuais para processamento ou distribuição separada.",
    "stepsTitle": "Como dividir um PDF",
    "step1Title": "Envie seu PDF",
    "step1Text": "Arraste e solte seu arquivo PDF ou clique para selecioná-lo do seu computador.",
    "step2Title": "Escolha o modo de divisão",
    "step2Text": "Selecione como dividir: extrair todas as páginas, definir intervalos personalizados ou clicar nas miniaturas para escolher páginas específicas.",
    "step3Title": "Divida e baixe",
    "step3Text": "Clique no botão Dividir para gerar os arquivos de saída. Baixe individualmente ou como arquivo ZIP.",
    "faq1Q": "Meus dados PDF são enviados a um servidor?",
    "faq1A": "Não. Todo o processamento acontece inteiramente no seu navegador. Seus arquivos PDF nunca saem do seu dispositivo.",
    "faq2Q": "Quais recursos do PDF são preservados após a divisão?",
    "faq2A": "Texto, imagens, fontes, anotações e campos de formulário são preservados. Alguns recursos avançados como ações JavaScript podem não ser transferidos.",
    "faq3Q": "Existe limite de tamanho de arquivo?",
    "faq3A": "Não há limite estrito, mas arquivos muito grandes (centenas de páginas) podem levar mais tempo para processar. A ferramenta manipula arquivos de qualquer tamanho que seu navegador possa manter na memória."
  }
}
```

- [ ] **Step 13: fr — Add to tools.json**

```json
"pdf-split": {
  "title": "Découper PDF - Séparer un PDF par pages ou plages",
  "shortTitle": "Découper PDF",
  "description": "Découpez un PDF en pages individuelles, plages personnalisées ou sélections manuelles. Aperçu des miniatures, téléchargement ZIP. Tout le traitement s'exécute dans votre navigateur."
}
```

- [ ] **Step 14: fr — Create pdf-split.json**

Create `public/locales/fr/pdf-split.json`:

```json
{
  "dropPdf": "Déposez un fichier PDF ici ou cliquez pour l'envoyer",
  "supportedFormats": "Uniquement les fichiers PDF",
  "onlyPdfSupported": "Seuls les fichiers PDF sont pris en charge",
  "modeExtractAll": "Extraire toutes les pages",
  "modeByRange": "Découper par plage",
  "modeSelectPages": "Sélectionner des pages",
  "rangeFrom": "De",
  "rangeTo": "À",
  "addRange": "Ajouter une plage",
  "removeRange": "Supprimer",
  "newGroup": "Nouveau groupe",
  "selectAll": "Tout sélectionner",
  "deselectAll": "Tout désélectionner",
  "splitButton": "Découper le PDF",
  "splitting": "Découpage en cours...",
  "splitProgress": "Traitement de la page {current} sur {total}...",
  "splitSuccess": "Découpage réussi !",
  "downloadZip": "Tout télécharger en ZIP",
  "download": "Télécharger",
  "newSplit": "Nouveau découpage",
  "page": "Page {num}",
  "pages": "Pages {from}-{to}",
  "pagesCount": "{count} pages",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} fichiers générés",
  "processing": "Découpage en cours...",
  "invalidRange": "La plage de pages doit être comprise entre 1 et {max}",
  "overlapRange": "Les plages de pages se chevauchent — les pages dans plusieurs plages apparaîtront dans chaque sortie",
  "encryptedPdf": "Ce PDF est chiffré et peut ne pas être découpé correctement",
  "corruptedPdf": "Ce fichier PDF est corrompu et ne peut pas être lu",
  "splitFailed": "Échec du découpage. Vérifiez votre fichier PDF et réessayez.",
  "descriptions": {
    "title": "À propos de Découper PDF",
    "aeoDefinition": "Découper PDF est un outil en ligne gratuit pour découper des fichiers PDF en pages individuelles, plages personnalisées ou sélections manuelles. Aperçu des miniatures, téléchargement individuel ou en ZIP. Tout le traitement s'exécute localement dans votre navigateur.",
    "whatIsTitle": "Qu'est-ce que Découper PDF ?",
    "whatIs": "Découpez un fichier PDF en pages individuelles ou plages personnalisées directement dans votre navigateur. Téléversez votre PDF, choisissez un mode de découpage (extraire toutes les pages, découper par plages ou sélectionner des pages spécifiques), prévisualisez les miniatures et téléchargez les résultats. Aucune donnée n'est envoyée à un serveur — tout le traitement utilise la bibliothèque pdf-lib.",
    "useCases": "Quand utiliser Découper PDF",
    "useCasesP1": "Extrayez des pages spécifiques d'un long rapport ou contrat pour ne partager que les sections pertinentes.",
    "useCasesP2": "Découpez un PDF de plusieurs pages en pages individuelles pour un traitement ou une distribution séparée.",
    "stepsTitle": "Comment découper un PDF",
    "step1Title": "Téléversez votre PDF",
    "step1Text": "Glissez-déposez votre fichier PDF ou cliquez pour le sélectionner depuis votre ordinateur.",
    "step2Title": "Choisissez le mode de découpage",
    "step2Text": "Sélectionnez comment découper : extraire toutes les pages, définir des plages personnalisées ou cliquer sur les miniatures pour choisir des pages spécifiques.",
    "step3Title": "Découpez et téléchargez",
    "step3Text": "Cliquez sur le bouton Découper pour générer les fichiers de sortie. Téléchargez-les individuellement ou sous forme d'archive ZIP.",
    "faq1Q": "Mes données PDF sont-elles envoyées à un serveur ?",
    "faq1A": "Non. Tout le traitement se fait entièrement dans votre navigateur. Vos fichiers PDF ne quittent jamais votre appareil.",
    "faq2Q": "Quelles fonctionnalités du PDF sont préservées après le découpage ?",
    "faq2A": "Le texte, les images, les polices, les annotations et les champs de formulaire sont préservés. Certaines fonctionnalités avancées comme les actions JavaScript peuvent ne pas être conservées.",
    "faq3Q": "Y a-t-il une limite de taille de fichier ?",
    "faq3A": "Il n'y a pas de limite stricte, mais les fichiers très volumineux (des centaines de pages) peuvent prendre plus de temps à traiter. L'outil gère les fichiers de toute taille que votre navigateur peut contenir en mémoire."
  }
}
```

- [ ] **Step 15: de — Add to tools.json**

```json
"pdf-split": {
  "title": "PDF Aufteilen - PDF nach Seiten oder Bereichen aufteilen",
  "shortTitle": "PDF Aufteilen",
  "description": "Teilen Sie ein PDF in einzelne Seiten, benutzerdefinierte Bereiche oder Handauswahl auf. Vorschaubilder, ZIP-Download. Die gesamte Verarbeitung läuft in Ihrem Browser."
}
```

- [ ] **Step 16: de — Create pdf-split.json**

Create `public/locales/de/pdf-split.json`:

```json
{
  "dropPdf": "PDF-Datei hier ablegen oder klicken zum Hochladen",
  "supportedFormats": "Nur PDF-Dateien werden unterstützt",
  "onlyPdfSupported": "Es werden nur PDF-Dateien unterstützt",
  "modeExtractAll": "Alle Seiten extrahieren",
  "modeByRange": "Nach Bereich aufteilen",
  "modeSelectPages": "Seiten auswählen",
  "rangeFrom": "Von",
  "rangeTo": "Bis",
  "addRange": "Bereich hinzufügen",
  "removeRange": "Entfernen",
  "newGroup": "Neue Gruppe",
  "selectAll": "Alle auswählen",
  "deselectAll": "Auswahl aufheben",
  "splitButton": "PDF aufteilen",
  "splitting": "Aufteilung...",
  "splitProgress": "Verarbeite Seite {current} von {total}...",
  "splitSuccess": "Erfolgreich aufgeteilt!",
  "downloadZip": "Alle als ZIP herunterladen",
  "download": "Herunterladen",
  "newSplit": "Neue Aufteilung",
  "page": "Seite {num}",
  "pages": "Seiten {from}-{to}",
  "pagesCount": "{count} Seiten",
  "splitResult": "{name} — {size}",
  "totalFiles": "{count} Dateien erzeugt",
  "processing": "Aufteilung...",
  "invalidRange": "Seitenbereich muss zwischen 1 und {max} liegen",
  "overlapRange": "Seitenbereiche überlappen — Seiten in mehreren Bereichen erscheinen in jeder Ausgabe",
  "encryptedPdf": "Dieses PDF ist verschlüsselt und wird möglicherweise nicht korrekt aufgeteilt",
  "corruptedPdf": "Diese PDF-Datei ist beschädigt und kann nicht gelesen werden",
  "splitFailed": "Aufteilung fehlgeschlagen. Überprüfen Sie Ihre PDF-Datei und versuchen Sie es erneut.",
  "descriptions": {
    "title": "Über PDF Aufteilen",
    "aeoDefinition": "PDF Aufteilen ist ein kostenloses Online-Tool zum Aufteilen von PDF-Dateien in einzelne Seiten, benutzerdefinierte Bereiche oder Handauswahl. Vorschaubilder, einzelner oder ZIP-Download. Die gesamte Verarbeitung läuft lokal in Ihrem Browser.",
    "whatIsTitle": "Was ist PDF Aufteilen?",
    "whatIs": "Teilen Sie eine PDF-Datei in einzelne Seiten oder benutzerdefinierte Bereiche direkt in Ihrem Browser auf. Laden Sie Ihr PDF hoch, wählen Sie einen Aufteilungsmodus (alle Seiten extrahieren, nach Bereichen aufteilen oder bestimmte Seiten auswählen), zeigen Sie Vorschaubilder an und laden Sie die Ergebnisse herunter. Es werden keine Daten an einen Server gesendet — die gesamte Verarbeitung verwendet die pdf-lib-Bibliothek.",
    "useCases": "Wann man PDF Aufteilen verwendet",
    "useCasesP1": "Extrahieren Sie bestimmte Seiten aus einem langen Bericht oder Vertrag, um nur die relevanten Abschnitte zu teilen.",
    "useCasesP2": "Teilen Sie ein mehrseitiges PDF in einzelne Seiten für separate Verarbeitung oder Verteilung auf.",
    "stepsTitle": "Wie man ein PDF aufteilt",
    "step1Title": "PDF hochladen",
    "step1Text": "Ziehen Sie Ihre PDF-Datei per Drag-and-Drop oder klicken Sie, um sie von Ihrem Computer auszuwählen.",
    "step2Title": "Aufteilungsmodus wählen",
    "step2Text": "Wählen Sie, wie aufgeteilt werden soll: alle Seiten extrahieren, benutzerdefinierte Bereiche definieren oder Vorschaubilder anklicken, um bestimmte Seiten auszuwählen.",
    "step3Title": "Aufteilen und herunterladen",
    "step3Text": "Klicken Sie auf Aufteilen, um die Ausgabedateien zu erzeugen. Laden Sie diese einzeln oder als ZIP-Archiv herunter.",
    "faq1Q": "Werden meine PDF-Daten an einen Server gesendet?",
    "faq1A": "Nein. Die gesamte Verarbeitung erfolgt lokal in Ihrem Browser. Ihre PDF-Dateien verlassen niemals Ihr Gerät.",
    "faq2Q": "Welche PDF-Funktionen bleiben nach der Aufteilung erhalten?",
    "faq2A": "Text, Bilder, Schriftarten, Anmerkungen und Formularfelder bleiben erhalten. Einige erweiterte Funktionen wie JavaScript-Aktionen werden möglicherweise nicht übernommen.",
    "faq3Q": "Gibt es eine Dateigrößenbeschränkung?",
    "faq3A": "Es gibt keine strenge Begrenzung, aber sehr große Dateien (Hunderte von Seiten) können länger dauern. Das Tool verarbeitet Dateien beliebiger Größe, die in den Arbeitsspeicher Ihres Browsers passen."
  }
}
```

- [ ] **Step 17: ru — Add to tools.json**

```json
"pdf-split": {
  "title": "Разделение PDF - Разделить PDF по страницам или диапазонам",
  "shortTitle": "Разделить PDF",
  "description": "Разделите PDF на отдельные страницы, пользовательские диапазоны или вручную выбранные страницы. Миниатюры, ZIP-скачивание. Вся обработка выполняется в браузере."
}
```

- [ ] **Step 18: ru — Create pdf-split.json**

Create `public/locales/ru/pdf-split.json`:

```json
{
  "dropPdf": "Перетащите PDF-файл сюда или нажмите для загрузки",
  "supportedFormats": "Поддерживаются только PDF-файлы",
  "onlyPdfSupported": "Поддерживаются только PDF-файлы",
  "modeExtractAll": "Извлечь все страницы",
  "modeByRange": "Разделить по диапазону",
  "modeSelectPages": "Выбрать страницы",
  "rangeFrom": "С",
  "rangeTo": "По",
  "addRange": "Добавить диапазон",
  "removeRange": "Удалить",
  "newGroup": "Новая группа",
  "selectAll": "Выбрать все",
  "deselectAll": "Снять выделение",
  "splitButton": "Разделить PDF",
  "splitting": "Разделение...",
  "splitProgress": "Обработка страницы {current} из {total}...",
  "splitSuccess": "Успешно разделено!",
  "downloadZip": "Скачать всё как ZIP",
  "download": "Скачать",
  "newSplit": "Новое разделение",
  "page": "Страница {num}",
  "pages": "Страницы {from}-{to}",
  "pagesCount": "{count} страниц",
  "splitResult": "{name} — {size}",
  "totalFiles": "Создано файлов: {count}",
  "processing": "Разделение...",
  "invalidRange": "Диапазон страниц должен быть от 1 до {max}",
  "overlapRange": "Диапазоны страниц пересекаются — страницы в нескольких диапазонах появятся в каждом выходном файле",
  "encryptedPdf": "Этот PDF зашифрован и может не разделиться корректно",
  "corruptedPdf": "Этот PDF-файл повреждён и не может быть прочитан",
  "splitFailed": "Ошибка разделения. Проверьте PDF-файл и попробуйте снова.",
  "descriptions": {
    "title": "О разделении PDF",
    "aeoDefinition": "Разделение PDF — бесплатный онлайн-инструмент для разделения PDF-файлов на отдельные страницы, пользовательские диапазоны или вручную выбранные страницы. Предпросмотр миниатюр, индивидуальное или ZIP-скачивание. Вся обработка выполняется локально в вашем браузере.",
    "whatIsTitle": "Что такое разделение PDF?",
    "whatIs": "Разделите PDF-файл на отдельные страницы или пользовательские диапазоны прямо в браузере. Загрузите PDF, выберите режим разделения (извлечь все страницы, разделить по диапазонам или выбрать конкретные страницы), просмотрите миниатюры и скачайте результаты. Данные не отправляются на сервер — вся обработка выполняется с помощью библиотеки pdf-lib.",
    "useCases": "Когда использовать разделение PDF",
    "useCasesP1": "Извлеките конкретные страницы из длинного отчёта или договора, чтобы поделиться только нужными разделами.",
    "useCasesP2": "Разделите многостраничный PDF на отдельные страницы для отдельной обработки или рассылки.",
    "stepsTitle": "Как разделить PDF",
    "step1Title": "Загрузите PDF",
    "step1Text": "Перетащите PDF-файл в зону загрузки или нажмите, чтобы выбрать его на компьютере.",
    "step2Title": "Выберите режим разделения",
    "step2Text": "Выберите способ разделения: извлечь все страницы, задать пользовательские диапазоны или нажать на миниатюры для выбора конкретных страниц.",
    "step3Title": "Разделите и скачайте",
    "step3Text": "Нажмите кнопку «Разделить» для создания выходных файлов. Скачайте их по отдельности или в виде ZIP-архива.",
    "faq1Q": "Мои PDF-данные отправляются на сервер?",
    "faq1A": "Нет. Вся обработка выполняется локально в вашем браузере. Ваши PDF-файлы никогда не покидают ваше устройство.",
    "faq2Q": "Какие функции PDF сохраняются после разделения?",
    "faq2A": "Текст, изображения, шрифты, аннотации и поля форм сохраняются. Некоторые расширенные функции, такие как действия JavaScript, могут не сохраниться.",
    "faq3Q": "Есть ли ограничение на размер файла?",
    "faq3A": "Строгого ограничения нет, но очень большие файлы (сотни страниц) могут обрабатываться дольше. Инструмент работает с файлами любого размера, помещающимися в память браузера."
  }
}
```

- [ ] **Step 19: Commit all non-English i18n**

```bash
git add public/locales/zh-CN/tools.json public/locales/zh-CN/pdf-split.json \
  public/locales/zh-TW/tools.json public/locales/zh-TW/pdf-split.json \
  public/locales/ja/tools.json public/locales/ja/pdf-split.json \
  public/locales/ko/tools.json public/locales/ko/pdf-split.json \
  public/locales/es/tools.json public/locales/es/pdf-split.json \
  public/locales/pt-BR/tools.json public/locales/pt-BR/pdf-split.json \
  public/locales/fr/tools.json public/locales/fr/pdf-split.json \
  public/locales/de/tools.json public/locales/de/pdf-split.json \
  public/locales/ru/tools.json public/locales/ru/pdf-split.json
git commit -m "feat(pdf-split): add i18n for 9 non-English locales"
```

---

### Task 8: Final Verification

**Files:** No changes — verification only.

- [ ] **Step 1: Run TypeScript check**

Run: `npx tsc --noEmit 2>&1 | head -40`

Expected: No errors related to pdf-split files. Pre-existing errors in other files are acceptable.

- [ ] **Step 2: Run tests**

Run: `npx vitest run libs/pdf-split/__tests__/split.test.ts`

Expected: All tests pass.

- [ ] **Step 3: Run full test suite to ensure no regressions**

Run: `npm run test`

Expected: All existing tests still pass. New pdf-split tests pass.

- [ ] **Step 4: Verify dev server starts**

Run: `npm run dev` (start then stop after confirming it compiles)

Expected: No compilation errors. The `/pdf-split` route loads without error.

---

## Self-Review

### 1. Spec Coverage

| Spec Requirement                 | Task                                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------------- |
| Extract Every Page mode          | Task 2 (split.ts), Task 4 (UI mode tab)                                                     |
| Split by Range mode              | Task 2 (split.ts), Task 4 (UI range inputs)                                                 |
| Select Pages mode                | Task 2 (split.ts), Task 4 (UI thumbnail grid + groups)                                      |
| ZIP download (fflate)            | Task 4 (downloadAsZip function)                                                             |
| Individual download              | Task 4 (downloadFile function)                                                              |
| Filenames with page range        | Task 2 (splitPdf naming logic)                                                              |
| Thumbnail rendering (pdfjs-dist) | Task 2 (thumbnail.ts)                                                                       |
| Lazy rendering >30 pages         | Noted in spec but simplified to batch render with concurrency in Task 4 — acceptable for v1 |
| Bounded concurrency (3)          | Task 4 (THUMBNAIL_CONCURRENCY = 3)                                                          |
| Route `/pdf-split`               | Task 1 (page.tsx)                                                                           |
| Tool registration                | Task 5 (tools.ts)                                                                           |
| JSON-LD structured data          | Task 1 (buildToolSchemas)                                                                   |
| 10-locale i18n                   | Task 6 (en) + Task 7 (9 locales)                                                            |
| Unit tests                       | Task 3                                                                                      |
| Memory cleanup on unmount        | Task 4 (useEffect cleanup with refs)                                                        |
| `data.slice(0)` buffer copy      | Task 2 (split.ts + thumbnail.ts)                                                            |
| `ignoreEncryption: true`         | Task 2 (split.ts + thumbnail.ts)                                                            |
| Three-component pattern          | Task 4 (Conversion, Description, PdfSplitPage)                                              |
| PrivacyBanner                    | Task 4 (PdfSplitPage)                                                                       |
| RelatedTools                     | Task 4 (PdfSplitPage)                                                                       |
| DescriptionSection               | Task 4 (Description component)                                                              |

### 2. Placeholder Scan

No TBD, TODO, "implement later", "add validation", or "handle edge cases" found. All steps contain complete code.

### 3. Type Consistency

- `SplitMode`, `SplitOptions`, `SplitResult`, `SplitProgress` — exported from `split.ts`, imported in `pdf-split-page.tsx`
- `getPdfPageCount(data: ArrayBuffer)` — matches `file.arrayBuffer()` return type
- `renderPageThumbnail(data: ArrayBuffer, pageIndex: number)` — 0-indexed as specified
- All i18n keys used in Task 4 match the keys defined in Task 6/7
